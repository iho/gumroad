import { observer } from "mobx-react-lite";
import React from 'react';

const Footer: React.FunctionComponent<{}> = observer(() => {
    return <footer> Footer (c) </footer>
  });

export default Footer;